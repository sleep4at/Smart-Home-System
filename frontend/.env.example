# 前端请求后端 API 的基础地址（只填域名/端口，不要带末尾斜杠）
# VITE_API_BASE_URL 有值
# 前端会直接请求这个后端地址；VITE_DEV_API_PROXY_TARGET 基本不参与。

# VITE_API_BASE_URL 为空
# 前端走相对路径 /api/...，这时才靠 VITE_DEV_API_PROXY_TARGET 转发。

# 例如：
# VITE_API_BASE_URL=http://127.0.0.1:8000
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=

# 仅本地开发时使用（vite dev server 的 /api 代理目标）
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8000

# MQTT 调试工具默认 WebSocket 地址
VITE_MQTT_WS_URL=ws://127.0.0.1:8083/mqtt
